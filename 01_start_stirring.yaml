version: "1.0"

experiment_profile_name: Start stirring

metadata:
  author: Cameron Davidson-Pilon
  description: This profile starts stirring immediately on all worker Pioreactors

common:
  jobs:
    stirring:
      actions:
        - type: start
          t: 0s
